{
  "name": "hardhat-project",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:test": "hardhat run --network sepolia-testnet scripts/deploy.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "ethers": "^6.13.5",
    "hardhat": "^2.19.3"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}
